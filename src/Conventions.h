typedef enum {
    X_DIR_EAST = 1,
    X_DIR_WEST = -1,
    X_DIR_NONE = 0
} xDir_t;

typedef enum {
    Y_DIR_NORTH = 1,
    Y_DIR_SOUTH = -1,
    Y_DIR_NONE = 0
} yDir_t;

typedef enum {
    COUNTER_CLOCKWISE = 1,
    CLOCKWISE = -1,
    SWIVEL_DIR_NONE = 0
} swivelDir_t;